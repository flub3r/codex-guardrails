[features]
multi_agent = true

[agents]
enabled = true
max_concurrent_threads_per_session = 6
# Deliberately do not pin the primary model here.
# Users choose the main Codex model; specialized workers below optimize context and cost.
